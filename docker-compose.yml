version: '3.2'
services:
  jekyll:
    image: jekyll/jekyll:latest
    command: jekyll serve --watch --force_polling --config _local_config.yml
    ports:
        - 8080:8080
        - 35729:35729
    volumes:
      - .:/srv/jekyll
