{%- assign data = site.data.config -%}
{%- comment -%}
{{data | jsonify}},
{%- endcomment -%}
{%- for hash in data -%}
  {
    "{{ hash[0] }}": {{ hash[1] | jsonify }}
  }
  {% unless forloop.last %},{% endunless %}
{%- endfor -%}
,{"cms_url": "{{ site.url }}"},
{"lms_url": "{{ site.lms_url }}"}
